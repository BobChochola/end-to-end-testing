Open the terminal :

Install Homebrew :


```/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
```
  2.  Install node :

```
brew install node
```
   3. Install WebDriver :

```
sudo webdriver-manager update --versions.chrome 2.46
```
  4. Replace to you are name and mail:

```
git config --global user.name "John Doe"
git config --global user.email johndoe@example.com
```
Change user.name and user.email to oneself

 5. Install Docker 

Go to this link download 

```
https://hub.docker.com/editions/community/docker-ce-desktop-mac
```
sign up Docker account

  6. Clone 'cucumber-testing'


https://bitbucket.org/starlinglabs/cucumber-testing/src/master/
Click ‘Clone’ , And Copy



git clone git@bitbucket.org:starlinglabs/cucumber-testing.git